# exp-render

Workload experiment - Caminho A (renderer/kustomize)

## Visão geral

TODO: descreva o que o serviço faz, suas rotas e como rodar localmente.

## Endpoints

- `GET /health` — liveness/readiness
- `GET /metrics` — métricas Prometheus

## Como rodar

```bash
npm install
npm run dev
```
